apiVersion: v1
kind: Service
metadata:
  name: computers-frontend-service
  labels:
    app: computers-frontend
spec:
  type: ClusterIP
  selector:
    app: computers-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
